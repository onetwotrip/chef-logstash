# Generated by Chef

<% syslog = node['logstash']['filters']['syslog'] -%>
filter {
  if [type] == "syslog" {
  <% [:grok, :date, :mutate].each do |k| -%>
    <%= logstash.plugin_pp(syslog[k], 4) %>
  <% end -%>
  }
}
